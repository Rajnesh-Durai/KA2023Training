JS:

USE OF LET KEYWORD IN FOR LOOP
MAP,FILTER,REDUCE FUNCTIONS
TEMPLATE LITERALS, USE OF .THIS KEYWORD, REST AND SPREAD OPERATOR
DESTUCTORS
FUNCTIONS AND ARROW FUNCTIONS
IMPORT AND EXPORT IN MODULES

ES6:

ARROW FUNCTIONS
CLASS ==> CONSTRUCTORS
INTERFACE

let arr1=["app,"game","cow"]

let mapset= arr1.map(obj=>obj+"s")
let mapset= arr1.filter(obj=>obj.length>2)

let arr2=[2,3,4,5,6]
let reduc=arr2.reduce((sum,currnt)=>sum+=current,0);//sum=2,current=3 then current=4 only current will iterate

let mapset= arr1.reduce(obj=>obj+"s")